import React from 'react'
import { HeaderStyled, HeaderTitle } from './Header.styles'

const Header = (props) => {
   return (
      <HeaderStyled>
         <HeaderTitle>Space-X Knowledge Base</HeaderTitle>
      </HeaderStyled>
   );
}

export default Header